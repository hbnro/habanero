# Edit and save, then execute:
# cat composer.txt | grep -v '#' > composer.json

{
  "require": {
    "php": ">=5.3"
  },
  "autoload": {
    "classmap": ["app/models"],
    "psr-0": {"": "library/"},
    "files": []
  }
}
